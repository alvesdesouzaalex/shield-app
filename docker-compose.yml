services:
  shield-app:
    build: .
    container_name: shield-app
    ports:
      - "9000:9000"

  nginx:
    image: nginx:latest
    container_name: nginx_app
    ports:
      - "80:80"
    volumes:
      - ./default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - shield-app
