# shield-app
This is an app to build a graphql app and build a shield with nginx


| Start shield-app 
```shell
 docker-compose up -d
```

| Rebuild shield-app
```shell
 docker-compose up --build -d
```